name: cardioformer-ckd
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.10
  - pytorch>=2.0
  - pytorch-cuda=12.1   # remove this line for CPU-only installs
  - numpy>=1.24
  - pandas>=2.0
  - scipy>=1.10
  - scikit-learn>=1.3
  - pyarrow>=12.0
  - pyyaml>=6.0
  - tqdm>=4.65
  - pip
  - pip:
      - wfdb>=4.1
      # - lifelines>=0.27   # optional: validated concordance index
